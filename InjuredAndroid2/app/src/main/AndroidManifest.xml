<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="b3nac.injuredandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".XSSTextActivity" />
        <activity android:name=".DisplayPostXSS" />
        <activity
            android:name=".BypassMainActivity"
            android:label="@string/title_activity_bypass_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".FlagOneSuccess" />
        <activity android:name=".b25lActivity"
                  android:exported="true">
        </activity>
    </application>

</manifest>